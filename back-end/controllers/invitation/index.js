const {deleteInvitation} = require('./deleteInvitation')
const {addInvitation} =  require('./addInvitation')
const {invitations} = require('./invitations')

module.exports = {invitations, addInvitation, deleteInvitation}